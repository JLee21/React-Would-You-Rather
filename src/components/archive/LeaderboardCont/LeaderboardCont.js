import React from 'react';
import { connect } from 'react-redux';

class LeaderboardCont extends React.Component {
  static propTypes = {

  }
  render = () => (
    <div>
      LeaderboardCont
    </div>
  )
}

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(LeaderboardCont);
